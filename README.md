# Buildpacks Slack Invite

This application powers <https://slack.buildpacks.io> to redirect to the Slack Buildpacks invite link.

## Usage

Set the environment variable `$REDIRECT_URL` to the slack invite URL.
